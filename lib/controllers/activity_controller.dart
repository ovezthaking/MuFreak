import 'package:flutter/material.dart';


class ActivityController extends StatelessWidget {
  const ActivityController({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}